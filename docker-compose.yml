version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: lila-game-codex:local
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      PORT: 3001
